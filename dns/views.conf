view "enterprise" {
  match-clients { enterprise-nets; };
  recursion yes;

  zone "enterprise.local" IN {
    type master;
    file "/etc/bind/zones/db.enterprise.local";
  };

  zone "corentinpradier.com" IN {
    type master;
    file "/etc/bind/zones/db.corentinpradier.com";
  };
};

view "residential" {
  match-clients { residential-nets; };
  recursion yes;

  zone "enterprise.local" IN {
    type master;
    file "/etc/bind/zones/db.enterprise.local.residential";
  };

  zone "corentinpradier.com" IN {
    type master;
    file "/etc/bind/zones/db.corentinpradier.com";
  };
};

view "default" {
  match-clients { any; };
  recursion no;
  allow-recursion { none; };

  zone "enterprise.local" IN {
    type master;
    file "/etc/bind/zones/db.enterprise.local";
  };

  zone "corentinpradier.com" IN {
    type master;
    file "/etc/bind/zones/db.corentinpradier.com";
  };
};
