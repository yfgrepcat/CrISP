# =============================================================================
# Enterprise OSPF + CRISP lab
# Intra-AS addressing carved from 120.0.32.0/20  (= 120.0.32.0 - 120.0.47.255)
#
# Loopbacks     -> 120.0.32.0/24
#   P1..P4         120.0.32.{1..4}/32
#   PE-nomad       120.0.32.22/32
#   PE-site        120.0.32.23/32
#
# Core P2P /31s -> 120.0.33.0/24
#   P1-P2          120.0.33.0/31    (P1 .0,  P2 .1)
#   P1-P3          120.0.33.2/31    (P1 .2,  P3 .3)
#   P1-P4          120.0.33.4/31    (P1 .4,  P4 .5)
#   P2-P3          120.0.33.6/31    (P2 .6,  P3 .7)
#   P2-P4          120.0.33.8/31    (P2 .8,  P4 .9)
#   P3-P4          120.0.33.10/31   (P3 .10, P4 .11)
#
# PE uplinks /31s -> 120.0.34.0/24
#   P2-PE-nomad    120.0.34.2/31    (P2 .2, PE-nomad .3)
#   P3-PE-site     120.0.34.4/31    (P3 .4, PE-site .5)
#   P2-RADIUS      120.0.34.10/31   (P2 .10, RADIUS .11)
#
# Lab root DNS /31 -> 120.0.36.4/31  (P2 .4, dns-root .5)
#   Dedicated transit segment on P2 for the lab root zone.
#
# VoIP /31s -> 120.0.35.0/24
#   P4-pbx         120.0.35.0/31    (P4 .0, pbx .1)         -- PBX branché sur P4
#   The softphones now live on the CRISP client net at 10.12.30.101/24 and 10.12.30.102/24.
#
# Service /31s -> 120.0.36.0/24
#   P1-DNS         120.0.36.0/31    (P1 .0, DNS .1)
#   P1-DHCP        120.0.36.2/31    (P1 .2, DHCP .3)
#
# CRISP router and downstream nets
#   PE-site-CRISP   120.0.39.0/31    (PE-site .0, CRISP .1)
#   CRISP DMZ VLAN  120.0.40.0/24    (CRISP .1, ovpn + web)
#   CRISP SVC VLAN  120.0.41.0/24    (CRISP .1, PBX + DHCP)
#   CRISP clients   10.12.30.0/24    (CRISP .1, client PC + softphones)
#
#   ovpn-site is single-homed in the CRISP DMZ (120.0.40.2/24). The OpenVPN
#   listener binds the DMZ IP; the nomad reaches it via internal OSPF
#   (home-ce sits on the residential access bridge net-nomad 120.0.38.0/24 → PE-nomad → core → PE-site →
#   CRISP → DMZ).
#
# Nomad home network (behind the CE — opaque to everything else)
#   net-home 192.168.1.0/24
#     home-ce eth2    192.168.1.1/24     (CE LAN, NAT gateway)
#     ovpn-nomad eth1 192.168.1.10/24    (CPE plugged into the CE)
#
# VPN tunnel (point-to-point, static-key)
#   10.255.255.0/30   (nomad .1, HQ .2)
#
# Host bridges (create on host before deploy):
#   sudo ip link add name net-site type bridge && sudo ip link set net-site up
#   sudo ip link add name net-home type bridge && sudo ip link set net-home up
#
# External breakout trunk:
#   TRUNK_IFACE=<host-nic> sudo -E ./scripts/create-host-bridges.sh
#   - host NIC trunk subinterface VLAN 104 -> breakout-trunk -> P4:Ethernet4
#   - host NIC trunk subinterface VLAN 121 -> br-vlan121 -> PE-nomad:e1-4
#   - host NIC trunk subinterface VLAN 122 -> br-vlan122 -> PE-site:e1-5
# =============================================================================

name: enterprise-ospf-bgp

mgmt:
  network: clab-mgmt
  ipv4-subnet: 172.20.20.0/24

topology:

  kinds:
    nokia_srlinux:
      image: ghcr.io/nokia/srlinux:24.10.7
      type: ixr-d2
    linux:
      image: alpine:3.20
    arista_veos:
      image: ${VEOS_IMAGE:=vrnetlab/arista_veos:4.31.0F}
      image-pull-policy: Never

  # ===[CORE-ROUTERS]==============================================================

  nodes:
    P1:
      kind: nokia_srlinux
      mgmt-ipv4: 172.20.20.11
      group: core
      startup-config: |
        set / interface ethernet-1/1 admin-state enable
        set / interface ethernet-1/1 subinterface 0 admin-state enable
        set / interface ethernet-1/1 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/1 subinterface 0 ipv4 address 120.0.33.0/31
        set / interface ethernet-1/2 admin-state enable
        set / interface ethernet-1/2 subinterface 0 admin-state enable
        set / interface ethernet-1/2 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/2 subinterface 0 ipv4 address 120.0.33.2/31
        set / interface ethernet-1/3 admin-state enable
        set / interface ethernet-1/3 subinterface 0 admin-state enable
        set / interface ethernet-1/3 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/3 subinterface 0 ipv4 address 120.0.33.4/31
        set / interface ethernet-1/9 admin-state enable
        set / interface ethernet-1/9 subinterface 0 admin-state enable
        set / interface ethernet-1/9 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/9 subinterface 0 ipv4 address 120.0.36.0/31
        set / interface ethernet-1/10 admin-state enable
        set / interface ethernet-1/10 subinterface 0 admin-state enable
        set / interface ethernet-1/10 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/10 subinterface 0 ipv4 address 120.0.34.0/31
        set / interface ethernet-1/11 admin-state enable
        set / interface ethernet-1/11 subinterface 0 admin-state enable
        set / interface ethernet-1/11 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/11 subinterface 0 ipv4 address 120.0.36.2/31
        set / interface system0 admin-state enable
        set / interface system0 subinterface 0 admin-state enable
        set / interface system0 subinterface 0 ipv4 admin-state enable
        set / interface system0 subinterface 0 ipv4 address 120.0.32.1/32
        set / network-instance default interface ethernet-1/1.0
        set / network-instance default interface ethernet-1/2.0
        set / network-instance default interface ethernet-1/3.0
        set / network-instance default interface ethernet-1/9.0
        set / network-instance default interface ethernet-1/10.0
        set / network-instance default interface ethernet-1/11.0
        set / network-instance default interface system0.0
        set / network-instance default next-hop-groups group vpn-return nexthop 1 ip-address 120.0.39.1
        set / network-instance default static-routes route 10.255.255.0/30 next-hop-group vpn-return
        set / network-instance default protocols ospf instance main admin-state enable
        set / network-instance default protocols ospf instance main version ospf-v2
        set / network-instance default protocols ospf instance main router-id 120.0.32.1
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface system0.0 passive true
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface ethernet-1/9.0 passive true
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface ethernet-1/11.0 passive true
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface ethernet-1/1.0 interface-type point-to-point
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface ethernet-1/2.0 interface-type point-to-point
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface ethernet-1/3.0 passive true
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface ethernet-1/3.0 interface-type point-to-point
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface ethernet-1/10.0 interface-type point-to-point

    P2:
      kind: nokia_srlinux
      mgmt-ipv4: 172.20.20.12
      group: core
      startup-config: |
        set / interface ethernet-1/1 admin-state enable
        set / interface ethernet-1/1 subinterface 0 admin-state enable
        set / interface ethernet-1/1 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/1 subinterface 0 ipv4 address 120.0.33.1/31
        set / interface ethernet-1/2 admin-state enable
        set / interface ethernet-1/2 subinterface 0 admin-state enable
        set / interface ethernet-1/2 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/2 subinterface 0 ipv4 address 120.0.33.6/31
        set / interface ethernet-1/3 admin-state enable
        set / interface ethernet-1/3 subinterface 0 admin-state enable
        set / interface ethernet-1/3 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/3 subinterface 0 ipv4 address 120.0.33.8/31
        set / interface ethernet-1/4 admin-state enable
        set / interface ethernet-1/4 subinterface 0 admin-state enable
        set / interface ethernet-1/4 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/4 subinterface 0 ipv4 address 120.0.34.6/31
        set / interface ethernet-1/5 admin-state enable
        set / interface ethernet-1/5 subinterface 0 admin-state enable
        set / interface ethernet-1/5 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/5 subinterface 0 ipv4 address 120.0.34.10/31
        set / interface ethernet-1/10 admin-state enable
        set / interface ethernet-1/10 subinterface 0 admin-state enable
        set / interface ethernet-1/10 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/10 subinterface 0 ipv4 address 120.0.34.2/31
        set / interface ethernet-1/11 admin-state enable
        set / interface ethernet-1/11 subinterface 0 admin-state enable
        set / interface ethernet-1/11 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/11 subinterface 0 ipv4 address 120.0.36.4/31
        set / interface system0 admin-state enable
        set / interface system0 subinterface 0 admin-state enable
        set / interface system0 subinterface 0 ipv4 admin-state enable
        set / interface system0 subinterface 0 ipv4 address 120.0.32.2/32
        set / network-instance default interface ethernet-1/1.0
        set / network-instance default interface ethernet-1/2.0
        set / network-instance default interface ethernet-1/3.0
        set / network-instance default interface ethernet-1/4.0
        set / network-instance default interface ethernet-1/5.0
        set / network-instance default interface ethernet-1/10.0
        set / network-instance default interface ethernet-1/11.0
        set / network-instance default interface system0.0
        set / network-instance default protocols ospf instance main admin-state enable
        set / network-instance default protocols ospf instance main version ospf-v2
        set / network-instance default protocols ospf instance main router-id 120.0.32.2
        set / network-instance default interface ethernet-1/3.0
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface system0.0 passive true
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface ethernet-1/1.0 interface-type point-to-point
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface ethernet-1/3.0 passive true
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface ethernet-1/2.0 interface-type point-to-point
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface ethernet-1/3.0 interface-type point-to-point
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface ethernet-1/4.0 interface-type point-to-point
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface ethernet-1/5.0 passive true
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface ethernet-1/10.0 interface-type point-to-point
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface ethernet-1/11.0 passive true

    P3:
      kind: nokia_srlinux
      mgmt-ipv4: 172.20.20.13
      group: core
      startup-config: |
        set / interface ethernet-1/1 admin-state enable
        set / interface ethernet-1/1 subinterface 0 admin-state enable
        set / interface ethernet-1/1 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/1 subinterface 0 ipv4 address 120.0.33.3/31
        set / interface ethernet-1/2 admin-state enable
        set / interface ethernet-1/2 subinterface 0 admin-state enable
        set / interface ethernet-1/2 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/2 subinterface 0 ipv4 address 120.0.33.7/31
        set / interface ethernet-1/3 admin-state enable
        set / interface ethernet-1/3 subinterface 0 admin-state enable
        set / interface ethernet-1/3 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/3 subinterface 0 ipv4 address 120.0.33.10/31
        set / interface ethernet-1/10 admin-state enable
        set / interface ethernet-1/10 subinterface 0 admin-state enable
        set / interface ethernet-1/10 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/10 subinterface 0 ipv4 address 120.0.34.4/31
        set / interface system0 admin-state enable
        set / interface system0 subinterface 0 admin-state enable
        set / interface system0 subinterface 0 ipv4 admin-state enable
        set / interface system0 subinterface 0 ipv4 address 120.0.32.3/32
        set / network-instance default interface ethernet-1/1.0
        set / network-instance default interface ethernet-1/2.0
        set / network-instance default interface ethernet-1/3.0
        set / network-instance default interface ethernet-1/10.0
        set / network-instance default interface system0.0
        set / network-instance default protocols ospf instance main admin-state enable
        set / network-instance default protocols ospf instance main version ospf-v2
        set / network-instance default protocols ospf instance main router-id 120.0.32.3
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface system0.0 passive true
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface ethernet-1/1.0 interface-type point-to-point
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface ethernet-1/2.0 interface-type point-to-point
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface ethernet-1/3.0 interface-type point-to-point
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface ethernet-1/10.0 interface-type point-to-point
  
    P4:
      kind: arista_veos
      mgmt-ipv4: 172.20.20.14
      group: core
      env:
        QEMU_MEMORY: "6144"
      startup-config: configs/P4.eos.cfg

  # ===[EDGE-ROUTERS]==============================================================

    PE-nomad:
      kind: nokia_srlinux
      mgmt-ipv4: 172.20.20.22
      group: edge
      startup-config: |
        set / interface ethernet-1/1 admin-state enable
        set / interface ethernet-1/1 subinterface 0 admin-state enable
        set / interface ethernet-1/1 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/1 subinterface 0 ipv4 address 120.0.34.3/31
        set / interface ethernet-1/4 admin-state enable
        set / interface ethernet-1/4 subinterface 0 admin-state enable
        set / interface ethernet-1/4 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/4 subinterface 0 ipv4 address 120.0.38.1/24
        set / interface ethernet-1/4 subinterface 0 ipv4 dhcp-relay admin-state enable
        set / interface ethernet-1/4 subinterface 0 ipv4 dhcp-relay server [ 120.0.36.3 ]
        set / interface ethernet-1/3 admin-state enable
        set / interface ethernet-1/3 subinterface 0 admin-state enable
        set / interface ethernet-1/3 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/3 subinterface 0 ipv4 address 120.0.35.4/31
        set / interface system0 admin-state enable
        set / interface system0 subinterface 0 admin-state enable
        set / interface system0 subinterface 0 ipv4 admin-state enable
        set / interface system0 subinterface 0 ipv4 address 120.0.32.22/32
        set / network-instance default interface ethernet-1/1.0
        set / network-instance default interface ethernet-1/4.0
        set / network-instance default interface ethernet-1/3.0
        set / network-instance default interface system0.0
        set / network-instance default protocols ospf instance main admin-state enable
        set / network-instance default protocols ospf instance main version ospf-v2
        set / network-instance default protocols ospf instance main router-id 120.0.32.22
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface system0.0 passive true
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface ethernet-1/1.0 interface-type point-to-point
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface ethernet-1/3.0 passive true
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface ethernet-1/4.0 passive true

    PE-site:
      kind: nokia_srlinux
      mgmt-ipv4: 172.20.20.23
      group: edge
      startup-config: |
        set / interface ethernet-1/1 admin-state enable
        set / interface ethernet-1/1 subinterface 0 admin-state enable
        set / interface ethernet-1/1 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/1 subinterface 0 ipv4 address 120.0.34.5/31
        set / interface ethernet-1/2 admin-state enable
        set / interface ethernet-1/2 subinterface 0 admin-state enable
        set / interface ethernet-1/2 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/2 subinterface 0 ipv4 address 120.0.37.1/24
        set / interface ethernet-1/3 admin-state enable
        set / interface ethernet-1/3 subinterface 0 admin-state enable
        set / interface ethernet-1/3 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/3 subinterface 0 ipv4 address 120.0.35.2/31
        set / interface ethernet-1/4 admin-state enable
        set / interface ethernet-1/4 subinterface 0 admin-state enable
        set / interface ethernet-1/4 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/4 subinterface 0 ipv4 address 120.0.39.0/31
        set / interface ethernet-1/5 admin-state enable
        set / interface ethernet-1/5 subinterface 0 admin-state enable
        set / interface ethernet-1/5 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/5 subinterface 0 ipv4 address 120.0.41.1/24
        set / interface system0 admin-state enable
        set / interface system0 subinterface 0 admin-state enable
        set / interface system0 subinterface 0 ipv4 admin-state enable
        set / interface system0 subinterface 0 ipv4 address 120.0.32.23/32
        set / network-instance default interface ethernet-1/1.0
        set / network-instance default interface ethernet-1/2.0
        set / network-instance default interface ethernet-1/3.0
        set / network-instance default interface ethernet-1/4.0
        set / network-instance default interface ethernet-1/5.0
        set / network-instance default interface system0.0
        set / network-instance default static-routes route 10.255.255.0/30 next-hop-group ovpn-next
        set / network-instance default static-routes route 192.168.1.10/32 next-hop-group ovpn-next
        set / network-instance default next-hop-groups group ovpn-next nexthop 1 ip-address 120.0.40.2
        set / network-instance default protocols ospf instance main admin-state enable
        set / network-instance default protocols ospf instance main version ospf-v2
        set / network-instance default protocols ospf instance main router-id 120.0.32.23
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface system0.0 passive true
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface ethernet-1/1.0 interface-type point-to-point
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface ethernet-1/2.0 passive true
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface ethernet-1/3.0 interface-type point-to-point
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface ethernet-1/4.0 interface-type point-to-point
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface ethernet-1/5.0 passive true

    CRISP:
      kind: nokia_srlinux
      mgmt-ipv4: 172.20.20.24
      group: edge
      startup-config: |
        set / interface ethernet-1/1 admin-state enable
        set / interface ethernet-1/1 subinterface 0 admin-state enable
        set / interface ethernet-1/1 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/1 subinterface 0 ipv4 address 120.0.39.1/31
        set / interface ethernet-1/2 admin-state enable
        set / interface ethernet-1/2 subinterface 0 admin-state enable
        set / interface ethernet-1/2 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/2 subinterface 0 ipv4 address 120.0.40.1/24
        set / interface ethernet-1/3 admin-state enable
        set / interface ethernet-1/3 subinterface 0 admin-state enable
        set / interface ethernet-1/3 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/3 subinterface 0 ipv4 address 120.0.41.1/24
        set / interface ethernet-1/4 admin-state enable
        set / interface ethernet-1/4 subinterface 0 admin-state enable
        set / interface ethernet-1/4 subinterface 0 ipv4 admin-state enable
        set / interface ethernet-1/4 subinterface 0 ipv4 address 10.12.30.1/24
        set / interface ethernet-1/4 subinterface 0 ipv4 dhcp-relay admin-state enable
        set / interface ethernet-1/4 subinterface 0 ipv4 dhcp-relay server [ 120.0.41.10 ]
        set / interface system0 admin-state enable
        set / interface system0 subinterface 0 admin-state enable
        set / interface system0 subinterface 0 ipv4 admin-state enable
        set / interface system0 subinterface 0 ipv4 address 120.0.32.24/32
        set / network-instance default interface ethernet-1/1.0
        set / network-instance default interface ethernet-1/2.0
        set / network-instance default interface ethernet-1/3.0
        set / network-instance default interface ethernet-1/4.0
        set / network-instance default interface system0.0
        set / network-instance default static-routes route 10.255.255.0/30 next-hop-group ovpn-next
        set / network-instance default next-hop-groups group ovpn-next nexthop 1 ip-address 120.0.40.2
        set / network-instance default protocols ospf instance main admin-state enable
        set / network-instance default protocols ospf instance main version ospf-v2
        set / network-instance default protocols ospf instance main router-id 120.0.32.24
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface system0.0 passive true
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface ethernet-1/1.0 interface-type point-to-point
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface ethernet-1/2.0 passive true
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface ethernet-1/3.0 passive true
        set / network-instance default protocols ospf instance main area 0.0.0.0 interface ethernet-1/4.0 passive true

    # ===[SERVICES]==============================================================

    # ---[DNS]-------------------------------------------------------------------
    dns-as12:
      kind: linux
      image: internetsystemsconsortium/bind9:9.21
      mgmt-ipv4: 172.20.20.30
      group: services
      binds:
        - ./dns:/etc/bind
      exec:
        - sh -lc 'if command -v apk >/dev/null 2>&1; then apk add --no-cache tcpdump; else apt-get update && DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends tcpdump && rm -rf /var/lib/apt/lists/*; fi'
        - sh -lc 'printf "nameserver 120.0.36.1\n" > /etc/resolv.conf'
        - ip link set eth1 up
        - ip addr add 120.0.36.1/31 dev eth1
        - ip route replace default via 120.0.36.0

    # ---[ROOT DNS]--------------------------------------------------------------
    # Lab "root" nameserver — authoritative for `.`, delegates each AS's zone
    # to that AS's own resolver. Shared sync point so every AS DNS can come up
    # independently and still resolve its peers via iterative resolution.
    # Reachable today through OSPF on P2.
    dns-root:
      kind: linux
      image: internetsystemsconsortium/bind9:9.21
      mgmt-ipv4: 172.20.20.40
      group: services
      binds:
        - ./dns/root:/etc/bind
      exec:
        - sh -lc 'if command -v apk >/dev/null 2>&1; then apk add --no-cache tcpdump; else apt-get update && DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends tcpdump && rm -rf /var/lib/apt/lists/*; fi'
        - ip link set eth1 up
        - ip addr add 120.0.36.5/31 dev eth1
        - ip route replace default via 120.0.36.4

    # ---[DHCP]------------------------------------------------------------------
    dhcp:
      kind: linux
      image: gns3/dhcp:latest
      cap-add:
        - NET_ADMIN
      binds:
        - ./dhcp:/opt/dhcp:ro
      cmd: /bin/sh -lc "sleep 1; for i in {1..30}; do ip link set eth1 up 2>/dev/null && break; sleep 0.5; done; ip addr add 120.0.36.3/31 dev eth1 2>/dev/null || true; ip route add 120.0.32.0/20 via 120.0.36.2 dev eth1 2>/dev/null || true; mkdir -p /etc/dnsmasq.d; cp /opt/dhcp/dnsmasq.conf /etc/dnsmasq.d/enterprise-dhcp.conf; dnsmasq --no-daemon --bind-dynamic --conf-file=/etc/dnsmasq.d/enterprise-dhcp.conf"

    NOMAD-CLIENT:
      kind: linux
      image: alpine:latest
      mgmt-ipv4: 172.20.20.54
      cmd: /bin/sh -lc "sleep infinity"
      binds:
        - ./scripts/udhcpc-resolvconf.sh:/opt/udhcpc-resolvconf.sh:ro
      exec:
        - apk add --no-cache iproute2 tcpdump
        - sh -lc 'ip route del default via 172.20.20.1 dev eth0 || true'
        - udhcpc -i eth1 -q -s /opt/udhcpc-resolvconf.sh

    # ---[RADIUS]----------------------------------------------------------------
    # Minimal FreeRADIUS server in the CRISP private services VLAN (120.0.41.0/24, RADIUS = .11).
    # Two test users (alice/bob) live in ./radius/authorize; NAS clients and the
    # shared secret are in ./radius/clients.conf. Listens on UDP 1812/1813.
    radius:
      kind: linux
      image: freeradius/freeradius-server:latest
      mgmt-ipv4: 172.20.20.36
      group: services
      binds:
        - ./radius/clients.conf:/etc/raddb/clients.conf:ro
        - ./radius/authorize:/etc/raddb/mods-config/files/authorize:ro
      # The base image lacks iproute2 and the mgmt network may not be ready the
      # instant exec fires, so retry the install until `ip` exists, then bring
      # up the data-plane interface and point the default route at the CRISP SVC VLAN.
      exec:
        - sh -lc 'for i in $(seq 1 30); do command -v ip >/dev/null 2>&1 && break; apt-get update >/dev/null 2>&1 && DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends iproute2 >/dev/null 2>&1; sleep 2; done; ip link set eth1 up; ip addr add 120.0.41.11/24 dev eth1 2>/dev/null || true; ip route replace default via 120.0.41.1'

    SITE-CLIENT:
      kind: linux
      image: alpine:latest
      mgmt-ipv4: 172.20.20.52
      cmd: /bin/sh -lc "sleep infinity"
      exec:
        - apk add --no-cache iproute2 tcpdump
        - sh -lc 'ip route del default via 172.20.20.1 dev eth0 || true'
        - ip addr add 120.0.37.100/24 dev eth1
        - ip route replace default via 120.0.37.1 dev eth1

    CRISP-CLIENT:
      kind: linux
      image: alpine:latest
      mgmt-ipv4: 172.20.20.56
      cmd: /bin/sh -lc "sleep infinity"
      binds:
        - ./scripts/udhcpc-resolvconf.sh:/opt/udhcpc-resolvconf.sh:ro
      exec:
        - apk add --no-cache iproute2 tcpdump freeradius-utils bind-tools
        - sh -lc 'ip route del default via 172.20.20.1 dev eth0 || true'
        - udhcpc -i eth1 -q -s /opt/udhcpc-resolvconf.sh

    # ---[OpenVPN]---------------------------------------------------------------
    # Pre-configured CPE: shipped to the nomad user, plugged into their home CE.
    # Single uplink (eth1) on net-home; default goes through home-ce, which
    # NATs out toward the residential access bridge net-nomad. Dials the CRISP DMZ IP (120.0.40.2) over UDP/1194,
    # reached via the AS IGP (no public-IP middleman).
    ovpn-nomad:
      kind: linux
      mgmt-ipv4: 172.20.20.41
      group: services
      cap-add:
        - NET_ADMIN
      devices:
        - /dev/net/tun:/dev/net/tun
      binds:
        - vpn/openvpn/static.key:/etc/openvpn/static.key:ro
        - vpn/openvpn/nomad.conf:/etc/openvpn/ovpn.conf:ro
      exec:
        - apk add --no-cache openvpn iproute2
        - ip addr add 192.168.1.10/24 dev eth1
        - sh -lc 'ip route del default via 172.20.20.1 dev eth0 || true'
        - ip route replace default via 192.168.1.1 dev eth1
        - sysctl -w net.ipv4.ip_forward=1
        - openvpn --config /etc/openvpn/ovpn.conf --daemon

    # CRISP OpenVPN concentrator: single-homed in the DMZ.
    #   eth1 -> net-crisp-dmz (120.0.40.2/24)   DMZ leg + UDP/1194 listener
    # Reached from outside via the AS IGP (no dedicated public IP).
    ovpn-site:
      kind: linux
      mgmt-ipv4: 172.20.20.42
      group: services
      cap-add:
        - NET_ADMIN
      devices:
        - /dev/net/tun:/dev/net/tun
      binds:
        - vpn/openvpn/static.key:/etc/openvpn/static.key:ro
        - vpn/openvpn/site.conf:/etc/openvpn/ovpn.conf:ro
      exec:
        - apk add --no-cache openvpn iproute2
        - ip addr add 120.0.40.2/24 dev eth1
        - ip route replace default via 120.0.40.1 dev eth1
        - sysctl -w net.ipv4.ip_forward=1
        - openvpn --config /etc/openvpn/ovpn.conf --daemon

    dhcp-crisp:
      kind: linux
      image: gns3/dhcp:latest
      cap-add:
        - NET_ADMIN
      binds:
        - ./crisp/dnsmasq.conf:/opt/dhcp/dnsmasq.conf:ro
      cmd: /bin/sh -lc "sleep 1; for i in {1..30}; do ip link set eth1 up 2>/dev/null && break; sleep 0.5; done; ip addr add 120.0.41.10/24 dev eth1 2>/dev/null || true; ip route add 10.12.30.0/24 via 120.0.41.1 dev eth1 2>/dev/null || true; mkdir -p /etc/dnsmasq.d; cp /opt/dhcp/dnsmasq.conf /etc/dnsmasq.d/crisp-dhcp.conf; dnsmasq --no-daemon --bind-dynamic --conf-file=/etc/dnsmasq.d/crisp-dhcp.conf"

    # Home CE router: simulates a consumer Internet box. NATs the home LAN
    # (192.168.1.0/24) out to the residential access segment on net-nomad.
    home-ce:
      kind: linux
      mgmt-ipv4: 172.20.20.43
      group: services
      cap-add:
        - NET_ADMIN
      binds:
        - ./scripts/udhcpc-resolvconf.sh:/opt/udhcpc-resolvconf.sh:ro
      exec:
        - apk add --no-cache iproute2 iptables udhcpc
        - sh -lc 'for i in $(seq 1 120); do ip link set eth1 up 2>/dev/null || true; ip link show dev eth1 2>/dev/null | grep -q LOWER_UP && udhcpc -O dns -O router -i eth1 -q -s /opt/udhcpc-resolvconf.sh && break; sleep 0.5; done'
        - sh -lc 'grep -q "^nameserver " /etc/resolv.conf 2>/dev/null || printf "nameserver 120.0.36.1\n" > /etc/resolv.conf'
        - ip route replace default via 120.0.38.1 dev eth1
        - ip addr add 192.168.1.1/24  dev eth2     # LAN (net-home)
        - sh -lc 'ip route del default via 172.20.20.1 dev eth0 || true'
        - sysctl -w net.ipv4.ip_forward=1
        - iptables -t nat -A POSTROUTING -o eth1 -j MASQUERADE

    # ===[VoIP]================================================================

    # VoIP
    pbx:
      kind: linux
      image: voip/asterisk
      mgmt-ipv4: 172.20.20.31
      binds:
        - voip/asterisk/sip.conf:/etc/asterisk/sip.conf
        - voip/asterisk/extensions.conf:/etc/asterisk/extensions.conf
      exec:
        - sh -lc 'printf "nameserver 120.0.36.1\n" > /etc/resolv.conf'
        - ip addr add 120.0.41.5/24 dev eth1
        - ip link set eth1 up
        - ip route replace default via 120.0.41.1

    phone-crisp1:
      kind: linux
      image: voip/client
      mgmt-ipv4: 172.20.20.32
      env:
        SIP_USER: "1001"
        SIP_SERVER: "voip.corentinpradier.com"
        SIP_PASS: secret1
      exec:
        - sh -lc 'printf "nameserver 120.0.36.1\n" > /etc/resolv.conf'
        - ip link set eth1 up
        - ip addr add 10.12.30.101/24 dev eth1
        - ip route replace default via 10.12.30.1 dev eth1

    phone-crisp2:
      kind: linux
      image: voip/client
      mgmt-ipv4: 172.20.20.33
      env:
        SIP_USER: "1002"
        SIP_SERVER: "voip.corentinpradier.com"
        SIP_PASS: secret2
      exec:
        - sh -lc 'printf "nameserver 120.0.36.1\n" > /etc/resolv.conf'
        - ip link set eth1 up
        - ip addr add 10.12.30.102/24 dev eth1
        - ip route replace default via 10.12.30.1 dev eth1


    # ===[WEB]================================================================
    # ----------------------------------------------------- Reverse proxy
    reverse-proxy:
      kind: linux
      image: reverse-proxy:latest
      mgmt-ipv4: 172.20.20.34
      env:
        NOMAD_NETWORK: "120.0.38.0/24"
        SITE_NETWORK: "120.0.37.0/24"
        VPN_CLIENT_NETWORK: "192.168.1.10/32"
        CRISP_CLIENT_NETWORK: "10.12.30.0/24"
      exec:
        - sh -lc 'printf "nameserver 120.0.36.1\n" > /etc/resolv.conf'
        - ip addr add 120.0.40.3/24 dev eth1
        - ip link set eth1 up
        - ip route replace default via 120.0.40.1

    # ----------------------------------------------------- Web servers
    web-server:
      kind: linux
      image: web:latest
      mgmt-ipv4: 172.20.20.35
      exec:
        - sh -lc 'printf "nameserver 120.0.36.1\n" > /etc/resolv.conf'
        - ip addr add 120.0.40.4/24 dev eth1
        - ip link set eth1 up
        - ip route replace default via 120.0.40.1


    # ===[HOSTS]==============================================================

    # Host/service bridges
    net-isp:
      kind: bridge      # "public Internet" — 203.0.113.0/24
    dns-net:
      kind: bridge      # DNS link on P1 — 120.0.36.0/31
    dhcp-net:
      kind: bridge      # DHCP link on P1 — 120.0.36.2/31
    net-nomad:
      kind: bridge      # direct PE-nomad client LAN — 120.0.38.0/24
    net-site:
      kind: bridge      # enterprise client LAN behind PE-site — 120.0.37.0/24
    net-crisp-dmz:
      kind: bridge      # CRISP DMZ VLAN — 120.0.40.0/24
    net-crisp-srv:
      kind: bridge      # CRISP private services VLAN — 120.0.41.0/24
    net-crisp-cli:
      kind: bridge      # CRISP private client net — 10.12.30.0/24
    net-home:
      kind: bridge      # nomad's home LAN behind home-ce — 192.168.1.0/24
    breakout-trunk:
      kind: bridge      # raw trunk toward the physical breakout switch
    br-vlan121:
      kind: bridge      # PE-nomad client LAN exposed on physical VLAN 121 — 120.0.38.0/24
    br-vlan122:
      kind: bridge      # physical breakout VLAN 122 toward PE-site

  links:
    # IGP Full Mesh Core
    - endpoints: ["P1:e1-1", "P2:e1-1"]   # 120.0.33.0/31
    - endpoints: ["P1:e1-2", "P3:e1-1"]   # 120.0.33.2/31
    - endpoints: ["P1:e1-3", "P4:eth1"]   # 120.0.33.4/31
    - endpoints: ["P2:e1-2", "P3:e1-2"]   # 120.0.33.6/31
    - endpoints: ["P2:e1-3", "P4:eth2"]   # 120.0.33.8/31
    - endpoints: ["P3:e1-3", "P4:eth3"]   # 120.0.33.10/31

    # PE uplinks
    - endpoints: ["P2:e1-10", "PE-nomad:e1-1"]  # 120.0.34.2/31
    - endpoints: ["P3:e1-10", "PE-site:e1-1"]   # 120.0.34.4/31
    - endpoints: ["radius:eth1", "net-crisp-srv:radius-port"]     # 120.0.41.11/24

    # P1 service links
    - endpoints: ["P1:e1-9", "dns-net:p1-dns-port"]
    - endpoints: ["dns-as12:eth1", "dns-net:dns-port"]
    - endpoints: ["P1:e1-11", "dhcp-net:p1-dhcp-port"]
    - endpoints: ["dhcp:eth1", "dhcp-net:dhcp-port"]

    # Enterprise client access + CRISP LAN
    - endpoints: ["SITE-CLIENT:eth1",  "net-site:site-client-port"]
    - endpoints: ["ovpn-site:eth1",  "net-crisp-dmz:ovpn-site-port"]
    - endpoints: ["reverse-proxy:eth1", "net-crisp-dmz:reverse-proxy-port"]
    - endpoints: ["web-server:eth1", "net-crisp-dmz:web-server-port"]
    - endpoints: ["pbx:eth1", "net-crisp-srv:pbx-port"]
    - endpoints: ["dhcp-crisp:eth1", "net-crisp-srv:crisp-dhcp-port"]
    - endpoints: ["CRISP:e1-2", "net-crisp-dmz:crisp-dmz-port"]
    - endpoints: ["CRISP:e1-3", "net-crisp-srv:crisp-srv-port"]
    - endpoints: ["CRISP:e1-4", "net-crisp-cli:crisp-client-port"]
    - endpoints: ["CRISP-CLIENT:eth1", "net-crisp-cli:crisp-client-host-port"]
    - endpoints: ["PE-nomad:e1-4", "net-nomad:pe-nomad-netnomad"] # 120.0.38.1/24
    - endpoints: ["NOMAD-CLIENT:eth1", "net-nomad:nomad-client-port"]

    # PE southbound to host bridges
    - endpoints: ["PE-site:e1-2",  "net-site:pe-site-port"]  # 120.0.37.1/24 (enterprise client LAN)
    - endpoints: ["PE-site:e1-5",  "br-vlan122:pe-site-vlan122"] # VLAN 122, 120.0.41.1/24
    - endpoints: ["PE-site:e1-4",  "CRISP:e1-1"]  # 120.0.39.0/31 (PE-site <-> CRISP transit)

    # Root DNS on P2
    - endpoints: ["P2:e1-11", "dns-root:eth1"]  # 120.0.36.4/31

    # Home CE WAN attached to the residential aggregation (PE-nomad)
    - endpoints: ["home-ce:eth1",    "net-nomad:home-ce-wan"]      # DHCP WAN via PE-nomad (120.0.38.0/24)
    - endpoints: ["home-ce:eth2",    "net-home:home-ce-lan"]      # 192.168.1.1/24

    # Nomad CPE plugs into the home LAN
    - endpoints: ["ovpn-nomad:eth1", "net-home:ovpn-nomad-port"]  # 192.168.1.10/24

    # VoIP
    - endpoints: ["phone-crisp1:eth1",  "net-crisp-cli:phone-crisp1-port"]   # 10.12.30.101/24
    - endpoints: ["phone-crisp2:eth1", "net-crisp-cli:phone-crisp2-port"]   # 10.12.30.102/24

    # Physical breakout trunk for P4 BGP interconnect VLANs 1012/1213
    - endpoints: ["P4:eth4", "breakout-trunk:p4-trunk"]
  # end
